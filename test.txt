someFunction()
{
    statement;
    statement + 1;
    b = a + 2;
    c = 5;
    c += (b + c);
    c /= b;
    return c;
}

foo()
{
    if (true)
    {
        a + (b - c);
        (a + b) / (b - c);
    }

    if (a)
        return a;
    else if (1 + 1)
        return b;
    else
        return c;
}

1*x + y;
a + b - c / d;
